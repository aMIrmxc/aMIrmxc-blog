---
import CommentsWrapper from "./CommentsWrapper.astro";

interface Props {
  dir: "ltr" | "rtl";
  post_id: string;
}

const { dir, post_id } = Astro.props;
---

<CommentsWrapper dir={dir} post_id={post_id} client:load />
