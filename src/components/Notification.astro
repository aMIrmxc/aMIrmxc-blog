<div id="notification" class="fixed top-5 right-5 bg-accent text-white p-4 rounded-lg shadow-lg opacity-0 transition-opacity duration-500">
  <p id="notification-message"></p>
</div>

<script>
  function showNotification(message: string, isError = false) {
    const notification = document.getElementById('notification');
    const notificationMessage = document.getElementById('notification-message');
    if (notification && notificationMessage) {
      notificationMessage.textContent = message;
      notification.classList.remove('bg-red-500', 'bg-accent');
      notification.classList.add(isError ? 'bg-red-500' : 'bg-accent');
      notification.classList.remove('opacity-0');
      setTimeout(() => {
        notification.classList.add('opacity-0');
      }, 3000);
    }
  }
  document.addEventListener('show-notification', (e: any) => {
    showNotification(e.detail.message, e.detail.isError);
  });
</script>